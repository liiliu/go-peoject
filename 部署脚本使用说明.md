# 部署脚本使用说明

## 📁 部署脚本列表

| 脚本 | 环境 | 说明 |
|------|------|------|
| `yun-test.sh` | 测试环境 | 部署到测试服务器 |
| `yun-prod.sh` | 生产环境 | 部署到生产服务器（需确认） |

## 🚀 快速使用

### 部署到测试环境

```bash
bash yun-test.sh
```

### 部署到生产环境

```bash
bash yun-prod.sh
```

**注意**：生产环境部署需要输入 `YES` 确认。

## ⚙️ 配置脚本

### 1. 配置测试环境（yun-test.sh）

编辑 `yun-test.sh` 文件顶部的配置：

```bash
# 服务器配置
SERVER_HOST="119.91.131.93"      # 测试服务器 IP
SERVER_PORT="61022"               # SSH 端口
SERVER_USER="root"                # 用户名
SERVER_PATH="/data/server/..."    # 部署目录
```

### 2. 配置生产环境（yun-prod.sh）

编辑 `yun-prod.sh` 文件顶部的配置：

```bash
# 服务器配置（生产环境）
SERVER_HOST="your-prod-server-ip"        # ⚠️ 修改为生产服务器 IP
SERVER_PORT=""                            # 生产服务器 SSH 端口
SERVER_USER="root"
SERVER_PATH="/data/server/your_project_prod"  # ⚠️ 修改为生产部署目录
```

## 📋 部署前检查清单

### 测试环境

- [ ] 已配置 `yun-test.sh` 中的服务器信息
- [ ] 已配置 `config/env/test.toml` 配置文件
- [ ] 已测试本地编译通过

### 生产环境

- [ ] **已配置** `yun-prod.sh` 中的服务器信息
- [ ] **已修改** `config/env/prod.toml` 所有默认配置
- [ ] **已设置** 数据库正确连接信息
- [ ] **已设置** Redis 密码
- [ ] **已修改** JWT Secret 为强密钥
- [ ] **已确认** debug = false
- [ ] **已确认** autoMigrate = false
- [ ] 已在测试环境验证功能正常

## 🎯 脚本工作流程

两个脚本都执行相同的 8 步流程：

```
1. 检查依赖 (go, tar, scp, ssh)
   ↓
2. 清理旧构建
   ↓
3. 创建构建目录
   ↓
4. 编译 Go 程序 (Linux/amd64)
   ↓
5. 复制环境配置文件
   ↓
6. 打包 (server.tgz)
   ↓
7. 上传到服务器
   ↓
8. 远程执行重启脚本
```

## 🔧 常用操作

### 查看部署后的服务状态

```bash
# 测试环境
ssh -p 61022 root@119.91.131.93 "cd /data/server/... && bash restart.sh status"

# 生产环境
ssh root@your-prod-server-ip "cd /data/server/... && bash restart.sh status"
```

### 查看日志

```bash
# 测试环境
ssh -p 61022 root@119.91.131.93 "cd /data/server/... && bash restart.sh logs api 50"

# 生产环境
ssh root@your-prod-server-ip "cd /data/server/... && bash restart.sh logs api 50"
```

## ⚠️ 重要提示

### 生产环境部署

1. **必须输入 YES 确认**（全大写）
2. **自动检查配置安全性**
   - 检查是否存在 `CHANGE-THIS` 未修改的默认值
   - 检查是否存在 `your-` 开头的占位符
3. **部署前请三思**
   - 确保在测试环境充分测试
   - 确保配置文件已完全修改
   - 建议在低流量时段部署

### 端口配置

- 如果服务器使用默认 SSH 端口 22，可以：
  - 留空：`SERVER_PORT=""`
  - 填写 22：`SERVER_PORT="22"`
- 自定义端口直接填写，如：`SERVER_PORT="61022"`

### 配置文件

脚本会自动使用对应的环境配置：

- `yun-test.sh` → `config/env/test.toml`
- `yun-prod.sh` → `config/env/prod.toml`

## 🔍 故障排查

### 编译失败

```bash
# 检查 Go 环境
go version

# 更新依赖
go mod tidy

# 本地测试编译
go build .
```

### 上传失败

```bash
# 测试连接
ssh -p 端口 用户@服务器IP "echo OK"

# 检查磁盘空间
ssh -p 端口 用户@服务器IP "df -h"
```

### 远程重启失败

```bash
# 查看服务器日志
ssh -p 端口 用户@服务器IP "cd /部署目录 && tail -n 50 logs/api.log"

# 手动启动测试
ssh -p 端口 用户@服务器IP "cd /部署目录 && ./server api"
```

## 📚 相关文档

- [完整部署指南](docs/DEPLOYMENT.md)
- [部署脚本手册](docs/DEPLOYMENT_SCRIPTS.md)
- [环境配置说明](config/env/README.md)
- [服务器管理脚本](restart.sh)

## 💡 最佳实践

1. **测试先行**：始终先在测试环境部署和验证
2. **配置检查**：部署前仔细检查配置文件
3. **备份习惯**：重要更新前备份数据库
4. **分批部署**：多服务器环境建议分批部署
5. **监控告警**：部署后立即检查服务状态和日志
6. **回滚准备**：准备好快速回滚方案

---

**更新日期**：2025-10-16  
**版本**：v2.0
